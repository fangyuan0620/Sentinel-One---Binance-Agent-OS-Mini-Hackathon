PORT=3000
# Optional. Public market data works without credentials.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
# Optional Agent OS integration endpoint (OAuth is handled by the MCP host).
BINANCE_AGENT_OS_URL=https://agent.binance.com/mcp/agentic

